/* 댓글 가장 많이 단 순위 1~3위의 이름 (내가 작성한 게시글의 댓글도 모두 포함) 많은 순으로 정렬 */


SELECT U.NAME, COUNT(*) AS C_COUNT FROM COMMENTS AS C
LEFT JOIN USERS AS U 
ON C.USER_ID = U.ID 
GROUP BY USER_ID ORDER BY C_COUNT DESC LIMIT 3;