#include<stdio.h>
#include<string.h>
int main(){
	int sum = 0, i, j;
	char n1[10], n2[10];
	
	scanf("%s %s", &n1, &n2);
	
	for(i = 0; i < strlen(n1); i++){
		int k;
		switch(n1[i]){
			case '':
				k = 
				break;
		}
	}
}
